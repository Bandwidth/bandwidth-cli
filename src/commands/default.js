const numbers = require("@bandwidth/numbers");
const printer = require('../printer')
const { BadInputError } = require('../errors');

module.exports.listDefaultAction = async () => {

}

module.exports.setDefaultAction = async () => {
}

module.exports.deleteDefaultAction = async (siteId, cmdObj) => {
}
